{
  "$schema": "https://deno.land/x/denon@2.4.7/schema.json",
  "scripts": {
    "start": {
      "cmd": "deno run --import-map=import_map.json --allow-env --allow-read --allow-net main.ts",
      "desc": "run my main.ts file"
    }
  }
}